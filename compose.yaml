services:
  dev:
    build: .
    ports:
      - "3000:3000"
      - "5173:5173"
    env_file:
      - path: .env
        required: false
    volumes:
      - .:/app
      - node_modules:/app/node_modules
    init: true

volumes:
  node_modules:
